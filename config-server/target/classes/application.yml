server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/2023ayush/hotel-config-repo.git
          default-label: main
          clone-on-start: true
